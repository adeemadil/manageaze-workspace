# Google Stitch MCP (Cursor reads this from your shell for `.cursor/mcp.json`)
# Copy to `.env` only if a tool you use loads it; Cursor typically needs the variable
# exported in your environment (shell profile, direnv, or launchctl for GUI apps).
MANAGEAZE_STITCH_API_KEY=
